<% @books.each do |book| %>
  <div>
    <p><%= image_tag 'no_image.jpg' %></p>
    Title
    <%= link_to book_path(book.id) do %>
      <p><%= book.title %></p>
    <% end %>
    Opinion
    <p><%= book.body %></p>
  </div>
<% end %>