<div>
  <p><%= @book.title %></p>
  <p><%= @book.body %></p>
  <p><%= image_tag 'no_image.jpg' %></p>
  <p><%= @book.user.name %></p>
  <% if @book.user == current_user %>
    <%= link_to "Destroy", book_path(@book), method: :delete %>
  <% end %>
</div>